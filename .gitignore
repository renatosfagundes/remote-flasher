# Secrets (SSH credentials, IPs) — NEVER commit
secrets.py

# User-specific settings (credentials, folder paths)
.credentials.json
.user_settings.json

# PyInstaller output
dist/
build/

# Python cache
__pycache__/
*.pyc
